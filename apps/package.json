{
  "name": "wellnessround-nc-apps",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"yarn --cwd apps/client dev\" \"yarn --cwd apps/server dev\"",
    "build": "concurrently \"yarn --cwd apps/client build\" \"yarn --cwd apps/server build\"",
    "start": "concurrently \"yarn --cwd apps/client start\" \"yarn --cwd apps/server start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.5.0",
    "concurrently": "^7.6.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
